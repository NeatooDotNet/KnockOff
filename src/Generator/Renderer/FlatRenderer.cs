// src/Generator/Renderer/FlatRenderer.cs
#nullable enable
using System.Collections.Generic;
using System.Linq;
using KnockOff;
using KnockOff.Builder;
using KnockOff.Model.Flat;
using KnockOff.Model.Shared;
using KnockOff.Renderer.Shared;

namespace KnockOff.Renderer;

/// <summary>
/// Renders FlatGenerationUnit to source code string.
/// Pure emission - no decisions, just output what the model says.
/// </summary>
internal static class FlatRenderer
{
	public static string Render(FlatGenerationUnit unit)
	{
		var w = new CodeWriter();

		w.Line("// <auto-generated/>");
		w.Line("#nullable enable");
		w.Line("#pragma warning disable SYSLIB0050 // Obsolete serialization API");
		w.Line();

		// Always include System.Linq - needed for .Sum() in overload TotalCallCount and generic methods
		w.Line("using System.Linq;");
		w.Line();

		// Namespace
		if (!string.IsNullOrEmpty(unit.Namespace))
		{
			w.Line($"namespace {unit.Namespace};");
			w.Line();
		}

		// Open containing type wrappers for nested classes
		foreach (var containingType in unit.ContainingTypes)
		{
			var accessMod = string.IsNullOrEmpty(containingType.AccessModifier)
				? ""
				: containingType.AccessModifier + " ";
			w.Line($"{accessMod}partial {containingType.Keyword} {containingType.Name}");
			w.Line("{");
		}

		// Class declaration with interfaces
		var interfaces = string.Join(", ", unit.InterfaceList);
		var typeParams = FormatTypeParameters(unit.TypeParameters);
		var constraints = FormatConstraints(unit.TypeParameters);

		// Calculate the class name with type parameters for delegate signatures
		var classNameWithTypeParams = $"{unit.ClassName}{typeParams}";

		// Base class for stub overrides
		var baseClass = $"{unit.ClassName}Base{typeParams}";

		using (w.Block($"partial class {unit.ClassName}{typeParams} : {baseClass}, {interfaces}, global::KnockOff.IKnockOffStub{constraints}"))
		{
			// Track rendered interceptors to avoid duplicates
			// (Multiple interface members may map to the same interceptor)
			var renderedInterceptorClasses = new HashSet<string>();

			// Track which interceptors use pre-compiled mode vs generated classes
			var preCompiledInterceptors = new Dictionary<string, string>(); // interceptorName -> pre-compiled type

			// Generic method interfaces if needed
			if (unit.HasGenericMethods)
				RenderGenericMethodInterfaces(w);

			// Interceptor classes (deduplicated by interceptor name)
			// For eligible members, use pre-compiled field declarations instead of generated classes
			var propertyOptions = new PropertyInterceptorRenderOptions(
				BaseIndent: 0,
				IncludeStrictParameter: true,
				StrictAccessExpression: "strict");
			foreach (var prop in unit.Properties)
			{
				if (renderedInterceptorClasses.Add(prop.InterceptorClassName))
				{
					var unifiedModel = ModelAdapters.ToUnifiedPropertyModel(prop);
					// For primary constructor classes with stub override properties,
					// we cannot generate a constructor to wire SetFallback, so fall back to generated class.
					var canUsePreCompiled = PreCompiledInterceptorRenderer.CanUsePreCompiled(unifiedModel)
						&& !(unit.HasPrimaryConstructor && prop.HasStubOverride);
					if (canUsePreCompiled)
					{
						// Pre-compiled: record the type, skip class generation
						preCompiledInterceptors[prop.InterceptorName] = PreCompiledInterceptorRenderer.GetPropertyInterceptorType(unifiedModel);
					}
					else
					{
						// Fallback: generate class as before
						PropertyInterceptorRenderer.RenderInterceptorClass(w, unifiedModel, propertyOptions);
					}
				}
			}

			var indexerOptions = new IndexerInterceptorRenderOptions(
				BaseIndent: 0,
				IncludeStrictParameter: true,
				StrictAccessExpression: "strict");
			// Group indexers by interceptor class name and render as a single multi-indexer interceptor
			var indexersByClass = unit.Indexers.GroupBy(i => i.InterceptorClassName);
			foreach (var group in indexersByClass)
			{
				if (renderedInterceptorClasses.Add(group.Key))
				{
					var unifiedModels = group.Select(ModelAdapters.ToUnifiedIndexerModel).ToList();
					if (PreCompiledInterceptorRenderer.CanUsePreCompiled(unifiedModels))
					{
						// Pre-compiled: record the type, skip class generation
						var first = group.First();
						preCompiledInterceptors[first.InterceptorName] = PreCompiledInterceptorRenderer.GetIndexerInterceptorType(unifiedModels[0]);
					}
					else
					{
						// Fallback: generate class as before
						IndexerInterceptorRenderer.RenderInterceptorClass(w, unifiedModels, indexerOptions);
					}
				}
			}

			// Render method interceptor classes using groups (handles overloads)
			// All method interceptors are fully generated classes inheriting from MethodInterceptorRuntime.
			// No pre-compiled method interceptor types are used (Phase 2 redesign).
			foreach (var group in unit.MethodGroups)
			{
				if (renderedInterceptorClasses.Add(group.InterceptorClassName))
				{
					var unifiedModel = ModelAdapters.ToUnifiedModel(group, unit.ClassName, typeParams);
					var options = new InterceptorRenderOptions(
						BaseIndent: 0,
						IncludeStrictParameter: true,
						StrictAccessExpression: "strict");
					MethodInterceptorRenderer.RenderInterceptorClass(w, unifiedModel, options);
				}
			}

			// Render user-defined method interceptor classes (full interceptor with When/Sequence support)
			foreach (var group in unit.StubOverrideGroups)
			{
				if (renderedInterceptorClasses.Add(group.InterceptorClassName))
				{
					var unifiedModel = ModelAdapters.ToUnifiedModel(group, unit.ClassName, typeParams);
					var options = new InterceptorRenderOptions(
						BaseIndent: 0,
						IncludeStrictParameter: true,
						StrictAccessExpression: "strict",
						StubOverrideFallback: true,
						StubTypeName: classNameWithTypeParams);
					MethodInterceptorRenderer.RenderInterceptorClass(w, unifiedModel, options);
				}
			}

			foreach (var handler in unit.GenericMethodHandlers)
			{
				if (renderedInterceptorClasses.Add(handler.InterceptorClassName))
					RenderGenericMethodHandler(w, handler, classNameWithTypeParams);
			}

			// Render generic stub override handler groups (for overloaded generic stub overrides)
			foreach (var handlerGroup in unit.GenericStubOverrideHandlerGroups)
			{
				if (renderedInterceptorClasses.Add(handlerGroup.InterceptorClassName))
					RenderGenericStubOverrideHandlerGroup(w, handlerGroup, classNameWithTypeParams);
			}

			foreach (var evt in unit.Events)
			{
				if (renderedInterceptorClasses.Add(evt.InterceptorClassName))
					RenderEventInterceptorClass(w, evt);
			}

			// Interceptor properties (public access to interceptors)
			RenderInterceptorProperties(w, unit, preCompiledInterceptors);

			// Standard members (Strict mode, Object accessor)
			RenderStandardMembers(w, unit);

			// Source(T) methods for each interface in the hierarchy
			RenderSourceMethods(w, unit, preCompiledInterceptors);

			// Constructor for stub override SetFallback wiring
			RenderConstructorIfNeeded(w, unit, preCompiledInterceptors, classNameWithTypeParams);

			// Build set of interceptor names that have multiple UNIQUE overloads (need suffixed Invoke)
			var multiOverloadInterceptors = new HashSet<string>(
				unit.MethodGroups
					.Where(g => g.Methods.Select(GetSignatureSuffix).Distinct().Count() > 1)
					.Select(g => g.InterceptorName));

			// Build set of stub override interceptor names that have multiple UNIQUE overloads (need suffixed RecordCall)
			var multiOverloadStubOverrideInterceptors = new HashSet<string>(
				unit.StubOverrideGroups
					.Where(g => g.Methods.Select(GetSignatureSuffix).Distinct().Count() > 1)
					.Select(g => g.InterceptorName));

			// Build set of generic stub override interceptor names that have multiple overloads (need suffixed RecordCall/Callback)
			// A generic handler group needs suffixes if it has multiple type arities OR multiple signatures per arity
			var multiOverloadGenericStubOverrideInterceptors = new HashSet<string>(
				unit.GenericStubOverrideHandlerGroups
					.Where(g => g.TypeArityGroups.Count > 1 ||
					            g.TypeArityGroups.Any(a => a.SignatureGroups.Count > 1))
					.Select(g => g.InterceptorName));

			// Determine which interceptor names are multi-indexer (more than one UNIQUE key type)
			// Diamond-inherited indexers with the same key type count as one.
			var multiIndexerInterceptors = new HashSet<string>();
			{
				var keyTypesByName = new Dictionary<string, HashSet<string>>();
				foreach (var indexer in unit.Indexers)
				{
					if (!keyTypesByName.ContainsKey(indexer.InterceptorName))
						keyTypesByName[indexer.InterceptorName] = new HashSet<string>();
					keyTypesByName[indexer.InterceptorName].Add(indexer.KeyTypeFriendlyName);
				}
				foreach (var kvp in keyTypesByName)
				{
					if (kvp.Value.Count > 1)
						multiIndexerInterceptors.Add(kvp.Key);
				}
			}

			// Explicit interface implementations (NOT deduplicated - one per interface member)
			foreach (var prop in unit.Properties)
				RenderPropertyImplementation(w, prop, preCompiledInterceptors);

			foreach (var indexer in unit.Indexers)
				RenderIndexerImplementation(w, indexer, multiIndexerInterceptors);

			foreach (var method in unit.Methods)
				RenderMethodImplementation(w, method, multiOverloadInterceptors, multiOverloadStubOverrideInterceptors, multiOverloadGenericStubOverrideInterceptors, unit.GenericStubOverrideHandlerGroups, preCompiledInterceptors);

			foreach (var evt in unit.Events)
				RenderEventImplementation(w, evt);
		}

		// Close containing type wrappers for nested classes
		for (int i = 0; i < unit.ContainingTypes.Count; i++)
		{
			w.Line("}");
		}

		return w.ToString();
	}

	/// <summary>
	/// Renders the base class file for standalone stubs.
	/// Contains virtual protected methods for each interface method, suffixed with '_'.
	/// Users can override these methods to provide default implementations.
	/// </summary>
	public static string RenderBaseClass(FlatGenerationUnit unit)
	{
		var w = new CodeWriter();

		w.Line("// <auto-generated/>");
		w.Line("#nullable enable");
		w.Line("#pragma warning disable SYSLIB0050 // Obsolete serialization API");
		w.Line();

		// Namespace
		if (!string.IsNullOrEmpty(unit.Namespace))
		{
			w.Line($"namespace {unit.Namespace};");
			w.Line();
		}

		// Open containing type wrappers for nested classes
		foreach (var containingType in unit.ContainingTypes)
		{
			var accessMod = string.IsNullOrEmpty(containingType.AccessModifier)
				? ""
				: containingType.AccessModifier + " ";
			w.Line($"{accessMod}partial {containingType.Keyword} {containingType.Name}");
			w.Line("{");
		}

		// Base class declaration with type parameters and constraints
		var typeParams = FormatTypeParameters(unit.TypeParameters);
		var constraints = FormatConstraints(unit.TypeParameters);

		using (w.Block($"public class {unit.ClassName}Base{typeParams}{constraints}"))
		{
			// Generate virtual protected methods for each non-generic interface method
			// Generic methods are excluded from the base class pattern per design decision
			//
			// When multiple interfaces have the same method signature but different return types
			// (e.g., ISet<T>.Add returns bool, ICollection<T>.Add returns void), we must pick one.
			// We prefer the non-void version as it provides more functionality.
			var methodsToRender = new Dictionary<string, FlatMethodModel>();

			foreach (var method in unit.Methods)
			{
				// Skip generic methods - they use .Of<T>() pattern only
				if (method.IsGenericMethod)
					continue;

				// Skip delegation targets (these are synthetic methods)
				if (method.DelegationTarget != null)
					continue;

				// Key by method name + parameter types ONLY (excluding return type)
				// C# does not allow overloading by return type, so we must pick one when
				// multiple interfaces have same method name/params but different returns
				var signatureKey = GetParameterOnlySignatureKey(method);

				if (methodsToRender.TryGetValue(signatureKey, out var existing))
				{
					// Conflict: same method signature from different interfaces
					// Prefer non-void return type (more informative for user)
					if (existing.IsVoid && !method.IsVoid)
					{
						methodsToRender[signatureKey] = method;
					}
					// Otherwise keep the first one (or the non-void one already stored)
				}
				else
				{
					methodsToRender[signatureKey] = method;
				}
			}

			// Render the selected methods
			foreach (var method in methodsToRender.Values)
			{
				RenderBaseClassMethod(w, method);
			}

			// Generate virtual protected properties for each interface property
			// When multiple interfaces have the same property name but different accessors
			// (e.g., IReadOnly.Name has getter only, IMutable.Name has get/set), we must pick one.
			// We prefer the version with MORE accessors (get/set > get-only or set-only).
			var propertiesToRender = new Dictionary<string, FlatPropertyModel>();

			foreach (var property in unit.Properties)
			{
				// Skip delegation targets (these are synthetic properties)
				if (property.DelegationTarget != null)
					continue;

				// Key by property name ONLY
				// C# does not allow overloading properties by return type or accessors
				var key = property.MemberName;

				if (propertiesToRender.TryGetValue(key, out var existing))
				{
					// Conflict: same property name from different interfaces
					// Prefer the version with MORE accessors
					var existingAccessorCount = (existing.HasGetter ? 1 : 0) + (existing.HasSetter ? 1 : 0);
					var newAccessorCount = (property.HasGetter ? 1 : 0) + (property.HasSetter ? 1 : 0);

					if (newAccessorCount > existingAccessorCount)
					{
						propertiesToRender[key] = property;
					}
					// If same accessor count, keep first (stable ordering)
				}
				else
				{
					propertiesToRender[key] = property;
				}
			}

			// Render the selected properties
			foreach (var property in propertiesToRender.Values)
			{
				RenderBaseClassProperty(w, property);
			}
		}

		// Close containing type wrappers for nested classes
		for (int i = 0; i < unit.ContainingTypes.Count; i++)
		{
			w.Line("}");
		}

		return w.ToString();
	}

	/// <summary>
	/// Gets a signature key for a method that only includes parameter types, not return type.
	/// Used for base class deduplication where C# doesn't allow overloading by return type.
	/// </summary>
	private static string GetParameterOnlySignatureKey(FlatMethodModel method)
	{
		if (method.Parameters.Count == 0)
			return $"{method.MethodName}_NoParams";

		var paramTypes = method.Parameters
			.Select(p => p.Type.Replace("global::", "").Replace(" ", ""))
			.ToArray();
		return $"{method.MethodName}_{string.Join("_", paramTypes)}";
	}

	/// <summary>
	/// Renders a single virtual protected method in the base class.
	/// Method name is suffixed with '_' to distinguish from interface method.
	/// Returns default! to allow compilation without override.
	/// </summary>
	private static void RenderBaseClassMethod(CodeWriter w, FlatMethodModel method)
	{
		var methodName = $"{method.MethodName}_";
		var returnType = method.IsVoid ? "void" : method.ReturnType;

		// Check if method has out parameters that need to be assigned
		var outParams = method.Parameters.Where(p => p.RefKind == Microsoft.CodeAnalysis.RefKind.Out).ToList();
		var hasOutParams = outParams.Count > 0;

		var methodSig = MethodInterceptorRenderer.FormatMethodSignatureForDoc(method.MethodName, method.Parameters, method.ReturnType, method.IsVoid);
		w.Line($"/// <summary>Override to provide default implementation for {methodSig}.</summary>");

		if (hasOutParams)
		{
			// Methods with out parameters need a block body to assign all out params
			w.Line($"protected virtual {returnType} {methodName}({method.ParameterDeclarations})");
			using (w.Braces())
			{
				// Assign all out parameters to default
				foreach (var outParam in outParams)
				{
					w.Line($"{outParam.EscapedName} = default!;");
				}

				// Return default if non-void
				if (!method.IsVoid)
				{
					w.Line("return default!;");
				}
			}
		}
		else if (method.IsVoid)
		{
			w.Line($"protected virtual {returnType} {methodName}({method.ParameterDeclarations}) {{ }}");
		}
		else
		{
			w.Line($"protected virtual {returnType} {methodName}({method.ParameterDeclarations}) => default!;");
		}
		w.Line();
	}

	/// <summary>
	/// Renders a single virtual protected property in the base class.
	/// Property name is suffixed with '_' to distinguish from interface property.
	/// Returns default! to allow compilation without override.
	/// </summary>
	private static void RenderBaseClassProperty(CodeWriter w, FlatPropertyModel property)
	{
		var propertyName = $"{property.MemberName}_";
		var returnType = property.ReturnType;

		var shortType = MethodInterceptorRenderer.ShortenTypeForDoc(property.ReturnType);
		var accessors = (property.HasGetter, property.HasSetter) switch
		{
			(true, true) => " {{ get; set; }}",
			(true, false) => " {{ get; }}",
			(false, true) => " {{ set; }}",
			_ => ""
		};
		w.Line($"/// <summary>Override to provide default implementation for {shortType} {property.MemberName}{accessors}.</summary>");

		if (property.HasGetter && property.HasSetter)
		{
			// Get/set property - full property syntax
			w.Line($"protected virtual {returnType} {propertyName} {{ get => default!; set {{ }} }}");
		}
		else if (property.HasGetter)
		{
			// Get-only property - expression-bodied
			w.Line($"protected virtual {returnType} {propertyName} => default!;");
		}
		else if (property.HasSetter)
		{
			// Set-only property - block syntax
			w.Line($"protected virtual {returnType} {propertyName} {{ set {{ }} }}");
		}
		w.Line();
	}

	#region Type Parameter Formatting

	private static string FormatTypeParameters(EquatableArray<TypeParameterModel> typeParameters)
	{
		if (typeParameters.Count == 0)
			return "";

		var names = string.Join(", ", typeParameters.Select(tp => tp.Name));
		return $"<{names}>";
	}

	private static string FormatConstraints(EquatableArray<TypeParameterModel> typeParameters)
	{
		if (typeParameters.Count == 0)
			return "";

		var clauses = typeParameters
			.Where(tp => !string.IsNullOrEmpty(tp.Constraints))
			.Select(tp => $" where {tp.Name} : {tp.Constraints}");

		return string.Join("", clauses);
	}

	#endregion

	#region Generic Method Interfaces

	private static void RenderGenericMethodInterfaces(CodeWriter w)
	{
		w.Line("/// <summary>Interface for tracking calls to generic methods.</summary>");
		using (w.Block("private interface IGenericMethodCallTracker"))
		{
			w.Line("int CallCount { get; }");
		}
		w.Line();

		w.Line("/// <summary>Interface for resetting state.</summary>");
		using (w.Block("private interface IResettable"))
		{
			w.Line("void Reset();");
		}
		w.Line();
	}

	#endregion

	#region Property Interceptor Class

	private static void RenderPropertyInterceptorClass(CodeWriter w, FlatPropertyModel prop, string className)
	{
		w.Line($"/// <summary>Tracks and configures behavior for {prop.MemberName}.</summary>");
		using (w.Block($"public sealed class {prop.InterceptorClassName}"))
		{
			if (prop.IsInitOnly)
			{
				RenderInitPropertyInterceptorContent(w, prop);
			}
			else
			{
				RenderRegularPropertyInterceptorContent(w, prop, className);
			}
		}
		w.Line();
	}

	private static void RenderInitPropertyInterceptorContent(CodeWriter w, FlatPropertyModel prop)
	{
		// Verifiable state
		w.Line("private bool _isVerifiable;");
		w.Line("private global::KnockOff.Called? _verifiableTimes;");
		w.Line("private bool _valueSet;");
		w.Line();

		w.Line("private " + prop.ReturnType + " _value = default!;");
		w.Line($"/// <summary>The configured value for {prop.MemberName}. Setting this marks the property as configured.</summary>");
		w.Line($"public {prop.ReturnType} Value");
		using (w.Braces())
		{
			w.Line("get => _value;");
			w.Line("set { _value = value; _valueSet = true; }");
		}
		w.Line();

		w.Line("private int _getCount;");
		w.Line("private int _setCount;");
		w.Line();

		w.Line("/// <summary>The value from the most recent setter call.</summary>");
		w.Line($"public {prop.NullableReturnType} LastSetValue {{ get; private set; }}");
		w.Line();

		w.Line("/// <summary>Records a getter access.</summary>");
		w.Line("public void RecordGet() => _getCount++;");
		w.Line();

		w.Line("/// <summary>Records a setter access.</summary>");
		w.Line($"public void RecordSet({prop.NullableReturnType} value) {{ _setCount++; LastSetValue = value; }}");
		w.Line();

		w.Line("/// <summary>Resets tracking state (counts, LastSetValue) but preserves configuration (Value) and verifiable marking.</summary>");
		w.Line("public void Reset() { _getCount = 0; _setCount = 0; LastSetValue = default; }");
		w.Line();

		// Verifiable methods (fluent)
		w.Line($"/// <summary>Marks this property for verification by Stub.Verify(). Returns this for fluent chaining.</summary>");
		w.Line($"public {prop.InterceptorClassName} Verifiable() {{ _isVerifiable = true; _verifiableTimes = null; return this; }}");
		w.Line();
		w.Line($"/// <summary>Marks this property for verification by Stub.Verify() with Called constraint. Returns this for fluent chaining.</summary>");
		w.Line($"public {prop.InterceptorClassName} Verifiable(global::KnockOff.Called times) {{ _isVerifiable = true; _verifiableTimes = times; return this; }}");
		w.Line();

		// Verify methods - throw on failure
		w.Line("/// <summary>Verifies the property was accessed at least once. Throws VerificationException if not.</summary>");
		w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
		w.Line();

		w.Line("/// <summary>Verifies total access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
		w.Line("public void Verify(global::KnockOff.Called times)");
		using (w.Braces())
		{
			w.Line("var totalCount = _getCount + _setCount;");
			w.Line("if (!times.Validate(totalCount))");
			w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", times, totalCount));");
		}
		w.Line();

		// VerifyGet methods
		w.Line("/// <summary>Verifies the getter was accessed at least once. Throws VerificationException if not.</summary>");
		w.Line("public void VerifyGet() => VerifyGet(global::KnockOff.Called.AtLeastOnce);");
		w.Line();

		w.Line("/// <summary>Verifies getter access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
		w.Line("public void VerifyGet(global::KnockOff.Called times)");
		using (w.Braces())
		{
			w.Line("if (!times.Validate(_getCount))");
			w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName} (get)\", times, _getCount));");
		}
		w.Line();

		// VerifySet methods
		w.Line("/// <summary>Verifies the setter was accessed at least once. Throws VerificationException if not.</summary>");
		w.Line("public void VerifySet() => VerifySet(global::KnockOff.Called.AtLeastOnce);");
		w.Line();

		w.Line("/// <summary>Verifies setter access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
		w.Line("public void VerifySet(global::KnockOff.Called times)");
		using (w.Braces())
		{
			w.Line("if (!times.Validate(_setCount))");
			w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName} (set)\", times, _setCount));");
		}
		w.Line();

		// Internal verification support for stub-level Verify/VerifyAll
		w.Line("/// <summary>Whether this property was marked with Verifiable().</summary>");
		w.Line("internal bool IsVerifiable => _isVerifiable;");
		w.Line();

		w.Line("/// <summary>Whether this property has been configured (Value set).</summary>");
		w.Line("internal bool IsConfigured => _valueSet;");
		w.Line();

		w.Line("/// <summary>Checks verification for Stub.Verify() - only checks if marked verifiable.</summary>");
		w.Line($"internal global::KnockOff.VerificationFailure? CheckVerification()");
		using (w.Braces())
		{
			w.Line("if (!_isVerifiable) return null;");
			w.Line("var times = _verifiableTimes ?? global::KnockOff.Called.AtLeastOnce;");
			w.Line("var totalCount = _getCount + _setCount;");
			w.Line($"return times.Validate(totalCount) ? null : new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", times, totalCount);");
		}
		w.Line();

		w.Line("/// <summary>Checks verification for Stub.VerifyAll() - checks if configured.</summary>");
		w.Line($"internal global::KnockOff.VerificationFailure? CheckVerificationAll()");
		using (w.Braces())
		{
			w.Line("if (!IsConfigured) return null;");
			w.Line("var totalCount = _getCount + _setCount;");
			w.Line($"return totalCount >= 1 ? null : new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", global::KnockOff.Called.AtLeastOnce, totalCount);");
		}
	}

	private static void RenderRegularPropertyInterceptorContent(CodeWriter w, FlatPropertyModel prop, string className)
	{
		// Source field for Source(T) feature - uses declaring interface type
		w.Line($"/// <summary>Source object to delegate to when no Get/Set is configured.</summary>");
		w.Line($"internal {prop.DeclaringInterface}? _source;");
		w.Line();

		// Verifiable state
		w.Line("private bool _isVerifiable;");
		w.Line("private global::KnockOff.Called? _verifiableTimes;");
		w.Line("private bool _valueSet;");
		w.Line();

		if (prop.HasGetter)
		{
			w.Line("private int _getCount;");
			w.Line();

			w.Line("/// <summary>Callback invoked when the getter is accessed. If set, its return value is used.</summary>");
			w.Line($"public global::System.Func<{prop.ReturnType}>? Get {{ get; set; }}");
			w.Line();
		}

		if (prop.HasSetter)
		{
			w.Line("private int _setCount;");
			w.Line();

			w.Line("/// <summary>The value from the most recent setter call.</summary>");
			w.Line($"public {prop.NullableReturnType} LastSetValue {{ get; private set; }}");
			w.Line();

			w.Line("/// <summary>Callback invoked when the setter is accessed.</summary>");
			w.Line($"public global::System.Action<{prop.ReturnType}>? Set {{ get; set; }}");
			w.Line();
		}

		// Value property for backing storage - setting Value marks the property as configured
		w.Line("private " + prop.ReturnType + " _value" + GetDefaultValueSuffix(prop.DefaultExpression));
		w.Line("/// <summary>Value returned by getter when Get is not set. Setting this marks the property as configured.</summary>");
		w.Line($"public {prop.ReturnType} Value");
		using (w.Braces())
		{
			w.Line("get => _value;");
			w.Line("set { _value = value; _valueSet = true; }");
		}
		w.Line();

		if (prop.HasGetter)
		{
			w.Line("/// <summary>Records a getter access.</summary>");
			w.Line("public void RecordGet() => _getCount++;");
			w.Line();
		}

		if (prop.HasSetter)
		{
			w.Line("/// <summary>Records a setter access.</summary>");
			w.Line($"public void RecordSet({prop.NullableReturnType} value) {{ _setCount++; LastSetValue = value; }}");
			w.Line();
		}

		// Reset method - clears tracking state but preserves configuration (Get/Set/Value) and verifiable marking
		w.Line("/// <summary>Resets tracking state (counts, LastSetValue) but preserves configuration (Get, Set, Value) and verifiable marking.</summary>");
		var resetParts = new System.Collections.Generic.List<string>();
		if (prop.HasGetter) resetParts.Add("_getCount = 0;");
		if (prop.HasSetter) resetParts.Add("_setCount = 0; LastSetValue = default;");
		resetParts.Add("_source = null;");
		w.Line($"public void Reset() {{ {string.Join(" ", resetParts)} }}");
		w.Line();

		// Verifiable methods (fluent)
		w.Line($"/// <summary>Marks this property for verification by Stub.Verify(). Returns this for fluent chaining.</summary>");
		w.Line($"public {prop.InterceptorClassName} Verifiable() {{ _isVerifiable = true; _verifiableTimes = null; return this; }}");
		w.Line();
		w.Line($"/// <summary>Marks this property for verification by Stub.Verify() with Called constraint. Returns this for fluent chaining.</summary>");
		w.Line($"public {prop.InterceptorClassName} Verifiable(global::KnockOff.Called times) {{ _isVerifiable = true; _verifiableTimes = times; return this; }}");
		w.Line();

		// Build total count expression based on available accessors
		var totalCountExpr = prop.HasGetter && prop.HasSetter
			? "_getCount + _setCount"
			: (prop.HasGetter ? "_getCount" : "_setCount");

		// Verify methods - throw on failure
		w.Line("/// <summary>Verifies the property was accessed at least once. Throws VerificationException if not.</summary>");
		w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
		w.Line();

		w.Line("/// <summary>Verifies total access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
		w.Line("public void Verify(global::KnockOff.Called times)");
		using (w.Braces())
		{
			w.Line($"var totalCount = {totalCountExpr};");
			w.Line("if (!times.Validate(totalCount))");
			w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", times, totalCount));");
		}
		w.Line();

		// VerifyGet methods
		if (prop.HasGetter)
		{
			w.Line("/// <summary>Verifies the getter was accessed at least once. Throws VerificationException if not.</summary>");
			w.Line("public void VerifyGet() => VerifyGet(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies getter access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void VerifyGet(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_getCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName} (get)\", times, _getCount));");
			}
			w.Line();
		}

		// VerifySet methods
		if (prop.HasSetter)
		{
			w.Line("/// <summary>Verifies the setter was accessed at least once. Throws VerificationException if not.</summary>");
			w.Line("public void VerifySet() => VerifySet(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies setter access count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void VerifySet(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_setCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{prop.MemberName} (set)\", times, _setCount));");
			}
			w.Line();
		}

		// Internal verification support for stub-level Verify/VerifyAll
		w.Line("/// <summary>Whether this property was marked with Verifiable().</summary>");
		w.Line("internal bool IsVerifiable => _isVerifiable;");
		w.Line();

		// IsConfigured - Value set OR Get/Set callback registered
		var isConfiguredParts = new System.Collections.Generic.List<string> { "_valueSet" };
		if (prop.HasGetter) isConfiguredParts.Add("Get != null");
		if (prop.HasSetter) isConfiguredParts.Add("Set != null");
		w.Line("/// <summary>Whether this property has been configured (Value set or callbacks registered).</summary>");
		w.Line($"internal bool IsConfigured => {string.Join(" || ", isConfiguredParts)};");
		w.Line();

		w.Line("/// <summary>Checks verification for Stub.Verify() - only checks if marked verifiable.</summary>");
		w.Line($"internal global::KnockOff.VerificationFailure? CheckVerification()");
		using (w.Braces())
		{
			w.Line("if (!_isVerifiable) return null;");
			w.Line("var times = _verifiableTimes ?? global::KnockOff.Called.AtLeastOnce;");
			w.Line($"var totalCount = {totalCountExpr};");
			w.Line($"return times.Validate(totalCount) ? null : new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", times, totalCount);");
		}
		w.Line();

		w.Line("/// <summary>Checks verification for Stub.VerifyAll() - checks if configured.</summary>");
		w.Line($"internal global::KnockOff.VerificationFailure? CheckVerificationAll()");
		using (w.Braces())
		{
			w.Line("if (!IsConfigured) return null;");
			w.Line($"var totalCount = {totalCountExpr};");
			w.Line($"return totalCount >= 1 ? null : new global::KnockOff.VerificationFailure(\"{prop.MemberName}\", global::KnockOff.Called.AtLeastOnce, totalCount);");
		}
	}

	private static string GetDefaultValueSuffix(string defaultExpression)
	{
		// The DefaultExpression from the model contains the complete expression (e.g., "default!", "new List<int>()")
		// We need to convert it to an initializer format with " = " prefix and ";" suffix
		if (string.IsNullOrEmpty(defaultExpression) || defaultExpression == "default!")
			return " = default!;";

		// For NewInstance strategy, the expression will be like "new SomeType()"
		return $" = {defaultExpression};";
	}

	#endregion

	#region Signature Suffix Helpers

	private static string GetSignatureSuffix(FlatMethodModel method)
	{
		var returnSuffix = UnifiedInterceptorBuilder.GetTypeSuffix(method.ReturnType);
		if (method.Parameters.Count == 0)
			return $"NoParams_{returnSuffix}";
		return string.Join("_", method.Parameters.Select(p => UnifiedInterceptorBuilder.GetTypeSuffix(p.Type))) + $"_{returnSuffix}";
	}

	#endregion

	#region Generic Method Handler

	private static void RenderGenericMethodHandler(CodeWriter w, FlatGenericMethodHandlerModel handler, string className)
	{
		var isMultiArity = handler.ArityGroups.Count > 1;
		// When there's only one arity group, no suffix needed (even for multi-type-param methods like Convert<TIn, TOut>)
		// When there are multiple arity groups, use _N suffix to disambiguate dictionaries
		string GetDictSuffix(int typeParamCount) => isMultiArity ? $"_{typeParamCount}" : "";

		w.Line($"/// <summary>Interceptor for {handler.MethodName} (generic method with Of&lt;T&gt;() access).</summary>");
		using (w.Block($"public sealed class {handler.InterceptorClassName}"))
		{
			// Each type arity gets its own dictionary
			foreach (var arity in handler.ArityGroups)
			{
				var dictSuffix = GetDictSuffix(arity.TypeParameterCount);
				w.Line($"private readonly global::System.Collections.Generic.Dictionary<{arity.KeyType}, object> _typedHandlers{dictSuffix} = new();");
			}
			w.Line();

			// Of<T>() method for each type arity
			foreach (var arity in handler.ArityGroups)
			{
				var dictSuffix = GetDictSuffix(arity.TypeParameterCount);
				w.Line($"/// <summary>Gets the typed handler for the specified type argument(s).</summary>");
				w.Line($"public {arity.TypedHandlerClassName}<{arity.TypeParameterNames}> Of<{arity.TypeParameterNames}>(){arity.ConstraintClauses}");
				using (w.Braces())
				{
					w.Line($"var key = {arity.KeyConstruction};");
					w.Line($"if (!_typedHandlers{dictSuffix}.TryGetValue(key, out var handler))");
					using (w.Braces())
					{
						w.Line($"handler = new {arity.TypedHandlerClassName}<{arity.TypeParameterNames}>();");
						w.Line($"_typedHandlers{dictSuffix}[key] = handler;");
					}
					w.Line($"return ({arity.TypedHandlerClassName}<{arity.TypeParameterNames}>)handler;");
				}
				w.Line();
			}

			// Aggregate tracking (private - use Verify for public API)
			if (!isMultiArity)
			{
				w.Line("private int TotalCallCount => _typedHandlers.Values.Sum(h => ((IGenericMethodCallTracker)h).CallCount);");
			}
			else
			{
				var dictNames = string.Join(".Concat(", handler.ArityGroups.Select((a, i) =>
					$"_typedHandlers{GetDictSuffix(a.TypeParameterCount)}.Values"));
				dictNames += string.Join("", Enumerable.Range(0, handler.ArityGroups.Count - 1).Select(_ => ")"));
				w.Line($"private int TotalCallCount => {dictNames}.Sum(h => ((IGenericMethodCallTracker)h).CallCount);");
			}
			w.Line();

			// CalledTypeArguments - only for single-arity, too complex for mixed
			if (!isMultiArity)
			{
				var arity = handler.ArityGroups.First();
				w.Line($"/// <summary>All type argument(s) that were used in calls.</summary>");
				w.Line($"public global::System.Collections.Generic.IReadOnlyList<{arity.KeyType}> CalledTypeArguments => _typedHandlers.Where(kvp => ((IGenericMethodCallTracker)kvp.Value).CallCount > 0).Select(kvp => kvp.Key).ToList();");
				w.Line();
			}

			// Reset method
			w.Line("/// <summary>Resets all typed handlers.</summary>");
			using (w.Block("public void Reset()"))
			{
				foreach (var arity in handler.ArityGroups)
				{
					var dictSuffix = GetDictSuffix(arity.TypeParameterCount);
					w.Line($"foreach (var handler in _typedHandlers{dictSuffix}.Values)");
					w.Line($"\t((IResettable)handler).Reset();");
					w.Line($"_typedHandlers{dictSuffix}.Clear();");
				}
			}
			w.Line();

			// Verify methods
			w.Line("/// <summary>Verifies method was called at least once with any type argument. Throws VerificationException if not.</summary>");
			w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies total call count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void Verify(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(TotalCallCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{handler.MethodName}\", times, TotalCallCount));");
			}
			w.Line();

			// Nested Typed Handler Classes (one per type arity)
			foreach (var arity in handler.ArityGroups)
			{
				RenderTypedHandlerClass(w, handler.MethodName, arity);
			}
		}
		w.Line();
	}

	private static void RenderTypedHandlerClass(CodeWriter w, string methodName, FlatGenericMethodArityGroup arity)
	{
		w.Line($"/// <summary>Typed handler for {methodName} with specific type arguments.</summary>");
		w.Line($"public sealed class {arity.TypedHandlerClassName}<{arity.TypeParameterNames}> : IGenericMethodCallTracker, IResettable, global::KnockOff.IMethodTracking{arity.ConstraintClauses}");
		using (w.Braces())
		{
			// Delegate
			w.Line($"/// <summary>Delegate for {methodName}.</summary>");
			w.Line(arity.DelegateSignature);
			w.Line();

			// Private callback field
			w.Line($"private {methodName}Delegate? _call;");
			w.Line();

			// CallCount - private field with explicit interface implementation for aggregation
			w.Line("internal int _callCount;");
			w.Line("int IGenericMethodCallTracker.CallCount => _callCount;");
			w.Line();

			// LastArg/LastArgs
			if (arity.NonGenericParams.Count == 1)
			{
				var param = arity.NonGenericParams.GetArray()![0];
				w.Line($"/// <summary>The '{param.Name}' argument from the most recent call.</summary>");
				w.Line($"public {param.NullableType} LastArg {{ get; private set; }}");
				w.Line();
			}
			else if (arity.NonGenericParams.Count > 1)
			{
				w.Line("/// <summary>The arguments from the most recent call.</summary>");
				w.Line($"public {arity.LastCallType}? LastArgs {{ get; private set; }}");
				w.Line();
			}

			// Return/Call method (returns IMethodTracking for consistency with regular method interceptors)
			var typedHandlerEntryPoint = "Call";
			w.Line("/// <summary>Sets the callback invoked when this method is called. Returns this handler for tracking.</summary>");
			w.Line($"public global::KnockOff.IMethodTracking {typedHandlerEntryPoint}({methodName}Delegate callback) {{ _call = callback; return this; }}");
			w.Line();

			// Callback property for internal use by invocation logic
			w.Line("/// <summary>Gets the configured callback (internal use).</summary>");
			w.Line($"internal {methodName}Delegate? Callback => _call;");
			w.Line();

			// RecordCall
			w.Line("/// <summary>Records a method call.</summary>");
			if (arity.NonGenericParams.Count == 0)
			{
				w.Line("public void RecordCall() => _callCount++;");
			}
			else if (arity.NonGenericParams.Count == 1)
			{
				var param = arity.NonGenericParams.GetArray()![0];
				w.Line($"public void RecordCall({param.NullableType} {param.EscapedName}) {{ _callCount++; LastArg = {param.EscapedName}; }}");
			}
			else
			{
				var paramList = string.Join(", ", arity.NonGenericParams.Select(p => $"{p.NullableType} {p.EscapedName}"));
				var tupleConstruction = string.Join(", ", arity.NonGenericParams.Select(p => p.EscapedName));
				w.Line($"public void RecordCall({paramList}) {{ _callCount++; LastArgs = ({tupleConstruction}); }}");
			}
			w.Line();

			// Reset
			w.Line("/// <summary>Resets all tracking state.</summary>");
			if (arity.NonGenericParams.Count == 0)
			{
				w.Line("public void Reset() { _callCount = 0; _call = null; }");
			}
			else if (arity.NonGenericParams.Count == 1)
			{
				w.Line("public void Reset() { _callCount = 0; LastArg = default; _call = null; }");
			}
			else
			{
				w.Line("public void Reset() { _callCount = 0; LastArgs = default; _call = null; }");
			}
			w.Line();

			// Verify methods - new API throws VerificationException
			w.Line("/// <summary>Verifies call count is at least once. Throws VerificationException if not.</summary>");
			w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies call count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void Verify(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_callCount))");
				w.Line("\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"method\", times, _callCount));");
			}
			w.Line();

			// Verifiable methods
			w.Line("/// <summary>Marks for verification by Stub.Verify(). Returns this for fluent chaining.</summary>");
			w.Line("public global::KnockOff.IMethodTracking Verifiable() => this;");
			w.Line();

			w.Line("/// <summary>Marks for verification by Stub.Verify() with Called constraint. Returns this for fluent chaining.</summary>");
			w.Line("public global::KnockOff.IMethodTracking Verifiable(global::KnockOff.Called times) => this;");
		}
	}

	#endregion

	#region Generic Stub Override Handler Group

	private static void RenderGenericStubOverrideHandlerGroup(CodeWriter w, FlatGenericMethodHandlerGroup group, string className)
	{
		w.Line($"/// <summary>Interceptor for {group.MethodName} (generic stub override with Of&lt;T&gt;() access).</summary>");
		using (w.Block($"public sealed class {group.InterceptorClassName}"))
		{
			// Each type arity gets its own dictionary
			foreach (var arity in group.TypeArityGroups)
			{
				var dictSuffix = arity.TypeParameterCount > 1 ? $"_{arity.TypeParameterCount}" : "";
				w.Line($"private readonly global::System.Collections.Generic.Dictionary<{arity.KeyType}, object> _typedHandlers{dictSuffix} = new();");
			}
			w.Line();

			// Of<T>() method for each type arity
			foreach (var arity in group.TypeArityGroups)
			{
				var dictSuffix = arity.TypeParameterCount > 1 ? $"_{arity.TypeParameterCount}" : "";
				w.Line($"/// <summary>Gets the typed handler for the specified type argument(s).</summary>");
				w.Line($"public {arity.TypedHandlerClassName}<{arity.TypeParameterNames}> Of<{arity.TypeParameterNames}>(){arity.ConstraintClauses}");
				using (w.Braces())
				{
					w.Line($"var key = {arity.KeyConstruction};");
					w.Line($"if (!_typedHandlers{dictSuffix}.TryGetValue(key, out var handler))");
					using (w.Braces())
					{
						w.Line($"handler = new {arity.TypedHandlerClassName}<{arity.TypeParameterNames}>();");
						w.Line($"_typedHandlers{dictSuffix}[key] = handler;");
					}
					w.Line($"return ({arity.TypedHandlerClassName}<{arity.TypeParameterNames}>)handler;");
				}
				w.Line();
			}

			// Aggregate tracking
			if (group.TypeArityGroups.Count == 1)
			{
				w.Line("private int TotalCallCount => _typedHandlers.Values.Sum(h => ((IGenericMethodCallTracker)h).CallCount);");
			}
			else
			{
				var dictNames = string.Join(".Concat(", group.TypeArityGroups.Select((a, i) =>
					$"_typedHandlers{(a.TypeParameterCount > 1 ? $"_{a.TypeParameterCount}" : "")}.Values"));
				dictNames += string.Join("", Enumerable.Range(0, group.TypeArityGroups.Count - 1).Select(_ => ")"));
				w.Line($"private int TotalCallCount => {dictNames}.Sum(h => ((IGenericMethodCallTracker)h).CallCount);");
			}
			w.Line();

			// CalledTypeArguments - only for single-arity, too complex for mixed
			if (group.TypeArityGroups.Count == 1)
			{
				var arity = group.TypeArityGroups.First();
				w.Line($"/// <summary>All type argument(s) that were used in calls.</summary>");
				w.Line($"public global::System.Collections.Generic.IReadOnlyList<{arity.KeyType}> CalledTypeArguments => _typedHandlers.Where(kvp => ((IGenericMethodCallTracker)kvp.Value).CallCount > 0).Select(kvp => kvp.Key).ToList();");
				w.Line();
			}

			// Reset method
			w.Line("/// <summary>Resets all typed handlers.</summary>");
			using (w.Block("public void Reset()"))
			{
				foreach (var arity in group.TypeArityGroups)
				{
					var dictSuffix = arity.TypeParameterCount > 1 ? $"_{arity.TypeParameterCount}" : "";
					w.Line($"foreach (var handler in _typedHandlers{dictSuffix}.Values)");
					w.Line($"\t((IResettable)handler).Reset();");
					w.Line($"_typedHandlers{dictSuffix}.Clear();");
				}
			}
			w.Line();

			// Verify methods
			w.Line("/// <summary>Verifies method was called at least once with any type argument. Throws VerificationException if not.</summary>");
			w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies total call count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void Verify(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(TotalCallCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{group.MethodName}\", times, TotalCallCount));");
			}
			w.Line();

			// Nested Typed Handler Classes (one per type arity)
			foreach (var arity in group.TypeArityGroups)
			{
				RenderGenericStubOverrideTypedHandlerClass(w, group.MethodName, arity);
			}
		}
		w.Line();
	}

	private static void RenderGenericStubOverrideTypedHandlerClass(CodeWriter w, string methodName, FlatGenericTypeArityGroup arity)
	{
		w.Line($"/// <summary>Typed handler for {methodName} with specific type arguments.</summary>");
		w.Line($"public sealed class {arity.TypedHandlerClassName}<{arity.TypeParameterNames}> : IGenericMethodCallTracker, IResettable, global::KnockOff.IMethodTracking{arity.ConstraintClauses}");
		using (w.Braces())
		{
			// Delegates (one per signature)
			foreach (var sig in arity.SignatureGroups)
			{
				w.Line($"/// <summary>Delegate for {methodName}{sig.SignatureSuffix}.</summary>");
				w.Line(sig.DelegateSignature);
			}
			w.Line();

			// Private callback fields (one per signature)
			foreach (var sig in arity.SignatureGroups)
			{
				w.Line($"private {sig.DelegateName}? _call{sig.SignatureSuffix};");
			}
			w.Line();

			// CallCount
			w.Line("internal int _callCount;");
			w.Line("int IGenericMethodCallTracker.CallCount => _callCount;");
			w.Line();

			// Per-signature LastArg/LastArgs storage
			foreach (var sig in arity.SignatureGroups)
			{
				var paramArray = sig.AllParameters.GetArray() ?? Array.Empty<ParameterModel>();
				if (paramArray.Length == 1)
				{
					w.Line($"/// <summary>The argument from the most recent call to {methodName}{sig.SignatureSuffix}.</summary>");
					w.Line($"public {sig.LastCallType} LastArg{sig.SignatureSuffix} {{ get; private set; }}");
				}
				else if (paramArray.Length > 1)
				{
					w.Line($"/// <summary>The arguments from the most recent call to {methodName}{sig.SignatureSuffix}.</summary>");
					w.Line($"public {sig.LastCallType}? LastArgs{sig.SignatureSuffix} {{ get; private set; }}");
				}
			}
			if (arity.SignatureGroups.Count > 0)
				w.Line();

			// Return/Call methods (one per signature)
			foreach (var sig in arity.SignatureGroups)
			{
				var sigEntryPoint = sig.IsVoid ? "Call" : "Return";
				w.Line($"/// <summary>Sets the callback invoked when this signature is called. Returns this handler for tracking.</summary>");
				w.Line($"public global::KnockOff.IMethodTracking {sigEntryPoint}{sig.SignatureSuffix}({sig.DelegateName} callback) {{ _call{sig.SignatureSuffix} = callback; return this; }}");
			}
			w.Line();

			// Callback properties (one per signature)
			foreach (var sig in arity.SignatureGroups)
			{
				w.Line($"/// <summary>Gets the configured callback for {methodName}{sig.SignatureSuffix} (internal use).</summary>");
				w.Line($"internal {sig.DelegateName}? Callback{sig.SignatureSuffix} => _call{sig.SignatureSuffix};");
			}
			w.Line();

			// RecordCall methods (one per signature)
			foreach (var sig in arity.SignatureGroups)
			{
				var paramArray = sig.AllParameters.GetArray() ?? Array.Empty<ParameterModel>();
				w.Line($"/// <summary>Records a method call for {methodName}{sig.SignatureSuffix}.</summary>");
				if (paramArray.Length == 0)
				{
					w.Line($"internal void RecordCall{sig.SignatureSuffix}() => _callCount++;");
				}
				else if (paramArray.Length == 1)
				{
					var param = paramArray[0];
					w.Line($"internal void RecordCall{sig.SignatureSuffix}({param.NullableType} {param.EscapedName}) {{ _callCount++; LastArg{sig.SignatureSuffix} = {param.EscapedName}; }}");
				}
				else
				{
					var paramList = string.Join(", ", paramArray.Select(p => $"{p.NullableType} {p.EscapedName}"));
					var tupleConstruction = string.Join(", ", paramArray.Select(p => p.EscapedName));
					w.Line($"internal void RecordCall{sig.SignatureSuffix}({paramList}) {{ _callCount++; LastArgs{sig.SignatureSuffix} = ({tupleConstruction}); }}");
				}
			}
			w.Line();

			// Reset
			w.Line("/// <summary>Resets all tracking state.</summary>");
			var resetParts = new List<string> { "_callCount = 0" };
			foreach (var sig in arity.SignatureGroups)
			{
				resetParts.Add($"_call{sig.SignatureSuffix} = null");
				var paramArray = sig.AllParameters.GetArray() ?? Array.Empty<ParameterModel>();
				if (paramArray.Length == 1)
					resetParts.Add($"LastArg{sig.SignatureSuffix} = default");
				else if (paramArray.Length > 1)
					resetParts.Add($"LastArgs{sig.SignatureSuffix} = default");
			}
			w.Line($"public void Reset() {{ {string.Join("; ", resetParts)}; }}");
			w.Line();

			// Verify methods
			w.Line("/// <summary>Verifies call count is at least once. Throws VerificationException if not.</summary>");
			w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies call count satisfies the Called constraint. Throws VerificationException if not.</summary>");
			w.Line("public void Verify(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_callCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException(new global::KnockOff.VerificationFailure(\"{methodName}\", times, _callCount));");
			}
			w.Line();

			// Verifiable methods
			w.Line("/// <summary>Marks for verification by Stub.Verify(). Returns this for fluent chaining.</summary>");
			w.Line("public global::KnockOff.IMethodTracking Verifiable() => this;");
			w.Line();

			w.Line("/// <summary>Marks for verification by Stub.Verify() with Called constraint. Returns this for fluent chaining.</summary>");
			w.Line("public global::KnockOff.IMethodTracking Verifiable(global::KnockOff.Called times) => this;");
		}
	}

	#endregion

	#region Event Interceptor Class

	private static void RenderEventInterceptorClass(CodeWriter w, FlatEventModel evt)
	{
		w.Line($"/// <summary>Interceptor for {evt.EventName} event.</summary>");
		using (w.Block($"public sealed class {evt.InterceptorClassName}"))
		{
			// Backing field
			w.Line($"private {evt.DelegateType}? _handler;");
			w.Line();

			// Add/Remove tracking
			w.Line("private int _addCount;");
			w.Line("private int _removeCount;");
			w.Line();

			w.Line("/// <summary>Whether any handlers are subscribed.</summary>");
			w.Line("public bool HasSubscribers => _handler != null;");
			w.Line();

			// RecordAdd/RecordRemove
			w.Line($"/// <summary>Records an event subscription.</summary>");
			w.Line($"public void RecordAdd({evt.DelegateType}? value) {{ _addCount++; _handler = ({evt.DelegateType}?)global::System.Delegate.Combine(_handler, value); }}");
			w.Line();

			w.Line($"/// <summary>Records an event unsubscription.</summary>");
			w.Line($"public void RecordRemove({evt.DelegateType}? value) {{ _removeCount++; _handler = ({evt.DelegateType}?)global::System.Delegate.Remove(_handler, value); }}");
			w.Line();

			// Raise method
			RenderEventRaiseMethod(w, evt);

			// Reset - clears tracking state but preserves verifiable marking
			w.Line("/// <summary>Resets tracking state (counts, handler) but preserves verifiable marking.</summary>");
			w.Line("public void Reset() { _addCount = 0; _removeCount = 0; _handler = null; }");
			w.Line();

			// Verification API for events
			w.Line("private bool _isVerifiable;");
			w.Line("private global::KnockOff.Called? _verifiableTimes;");
			w.Line();

			w.Line("/// <summary>Verifies the event was subscribed to at least once.</summary>");
			w.Line("public void VerifyAdd() => VerifyAdd(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies the event subscription count matches the Called constraint.</summary>");
			w.Line("public void VerifyAdd(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_addCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException($\"Event '{evt.EventName}' add verification failed: expected {{times}}, but was called {{_addCount}} time(s).\");");
			}
			w.Line();

			w.Line("/// <summary>Verifies the event was unsubscribed at least once.</summary>");
			w.Line("public void VerifyRemove() => VerifyRemove(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies the event unsubscription count matches the Called constraint.</summary>");
			w.Line("public void VerifyRemove(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("if (!times.Validate(_removeCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException($\"Event '{evt.EventName}' remove verification failed: expected {{times}}, but was called {{_removeCount}} time(s).\");");
			}
			w.Line();

			w.Line("/// <summary>Verifies the event was accessed (add or remove) at least once.</summary>");
			w.Line("public void Verify() => Verify(global::KnockOff.Called.AtLeastOnce);");
			w.Line();

			w.Line("/// <summary>Verifies the total event access count matches the Called constraint.</summary>");
			w.Line("public void Verify(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("var totalCount = _addCount + _removeCount;");
				w.Line("if (!times.Validate(totalCount))");
				w.Line($"\tthrow new global::KnockOff.VerificationException($\"Event '{evt.EventName}' verification failed: expected {{times}}, but was called {{totalCount}} time(s).\");");
			}
			w.Line();

			w.Line("/// <summary>Marks this event for verification by Stub.Verify(). Returns this for fluent chaining.</summary>");
			w.Line($"public {evt.InterceptorClassName} Verifiable()");
			using (w.Braces())
			{
				w.Line("_isVerifiable = true;");
				w.Line("_verifiableTimes = global::KnockOff.Called.AtLeastOnce;");
				w.Line("return this;");
			}
			w.Line();

			w.Line("/// <summary>Marks this event for verification by Stub.Verify() with Called constraint. Returns this for fluent chaining.</summary>");
			w.Line($"public {evt.InterceptorClassName} Verifiable(global::KnockOff.Called times)");
			using (w.Braces())
			{
				w.Line("_isVerifiable = true;");
				w.Line("_verifiableTimes = times;");
				w.Line("return this;");
			}
			w.Line();

			// Internal verification methods for stub-level Verify()/VerifyAll()
			w.Line("internal bool IsVerifiable => _isVerifiable;");
			w.Line("internal bool IsConfigured => _handler != null;");
			w.Line();

			w.Line("/// <summary>Checks verification for Stub.Verify() - only verifiable items.</summary>");
			w.Line("internal global::KnockOff.VerificationFailure? CheckVerification()");
			using (w.Braces())
			{
				w.Line("if (!_isVerifiable) return null;");
				w.Line("var times = _verifiableTimes ?? global::KnockOff.Called.AtLeastOnce;");
				w.Line("var totalCount = _addCount + _removeCount;");
				w.Line("if (!times.Validate(totalCount))");
				w.Line($"\treturn new global::KnockOff.VerificationFailure(\"{evt.EventName}\", times, totalCount);");
				w.Line("return null;");
			}
			w.Line();

			w.Line("/// <summary>Checks verification for Stub.VerifyAll() - all configured items.</summary>");
			w.Line("internal global::KnockOff.VerificationFailure? CheckVerificationAll()");
			using (w.Braces())
			{
				w.Line("if (!IsConfigured && !_isVerifiable) return null;");
				w.Line("var times = _verifiableTimes ?? global::KnockOff.Called.AtLeastOnce;");
				w.Line("var totalCount = _addCount + _removeCount;");
				w.Line("if (!times.Validate(totalCount))");
				w.Line($"\treturn new global::KnockOff.VerificationFailure(\"{evt.EventName}\", times, totalCount);");
				w.Line("return null;");
			}
		}
		w.Line();
	}

	private static void RenderEventRaiseMethod(CodeWriter w, FlatEventModel evt)
	{
		if (evt.RaiseReturnsValue)
		{
			// Func-style delegate
			w.Line("/// <summary>Raises the event with the specified arguments and returns the result.</summary>");
			if (string.IsNullOrEmpty(evt.RaiseParameters))
			{
				w.Line($"public {evt.RaiseReturnType} Raise() => _handler != null ? _handler.Invoke() : default!;");
			}
			else
			{
				w.Line($"public {evt.RaiseReturnType} Raise({evt.RaiseParameters}) => _handler != null ? _handler.Invoke({evt.RaiseArguments}) : default!;");
			}
		}
		else if (evt.UsesDynamicInvoke)
		{
			// Custom delegate - use DynamicInvoke
			if (string.IsNullOrEmpty(evt.RaiseParameters))
			{
				w.Line("/// <summary>Raises the event.</summary>");
				w.Line("public void Raise() => (_handler as global::System.Action)?.Invoke();");
			}
			else
			{
				w.Line("/// <summary>Invokes the handler if subscribed.</summary>");
				w.Line($"public void Raise({evt.RaiseParameters}) => _handler?.DynamicInvoke({evt.RaiseArguments});");
			}
		}
		else
		{
			// Standard Action/EventHandler
			if (string.IsNullOrEmpty(evt.RaiseParameters))
			{
				w.Line("/// <summary>Raises the event.</summary>");
				w.Line("public void Raise() => _handler?.Invoke();");
			}
			else
			{
				w.Line("/// <summary>Raises the event with the specified arguments.</summary>");
				w.Line($"public void Raise({evt.RaiseParameters}) => _handler?.Invoke({evt.RaiseArguments});");
			}
		}
		w.Line();
	}

	#endregion

	#region Interceptor Properties

	private static void RenderInterceptorProperties(CodeWriter w, FlatGenerationUnit unit, Dictionary<string, string> preCompiledInterceptors)
	{
		// Track rendered interceptor properties to avoid duplicates
		var renderedProperties = new HashSet<string>();

		// Properties
		foreach (var prop in unit.Properties)
		{
			if (!renderedProperties.Add(prop.InterceptorName))
				continue;
			var newKeyword = prop.NeedsNewKeyword ? "new " : "";
			if (preCompiledInterceptors.TryGetValue(prop.InterceptorName, out var preCompiledType))
			{
				// Only compute smart default factory for properties with getters (set-only properties have no return value)
				var factory = prop.HasGetter
					? PreCompiledInterceptorRenderer.GetPropertySmartDefaultFactory(
						prop.ReturnType, prop.DefaultStrategy, prop.ConcreteTypeForNew, prop.MemberName)
					: null;
				var ctorArgs = PreCompiledInterceptorRenderer.GetFieldConstructorArgs(prop.MemberName, factory);
				w.Line($"/// <summary>Interceptor for {prop.MemberName}. Configure via .Value, track via .GetCount.</summary>");
				w.Line($"public {newKeyword}{preCompiledType} {prop.InterceptorName} {{ get; }} = new{ctorArgs};");
			}
			else
			{
				w.Line($"/// <summary>Interceptor for {prop.MemberName}. Configure via .Value, track via .GetCount.</summary>");
				w.Line($"public {newKeyword}{prop.InterceptorClassName} {prop.InterceptorName} {{ get; }} = new();");
			}
			w.Line();
		}

		// Indexers - each indexer gets its own property (no container)
		foreach (var indexer in unit.Indexers)
		{
			if (!renderedProperties.Add(indexer.InterceptorName))
				continue;
			var newKeyword = indexer.NeedsNewKeyword ? "new " : "";
			if (preCompiledInterceptors.TryGetValue(indexer.InterceptorName, out var preCompiledType))
			{
				var factory = PreCompiledInterceptorRenderer.GetIndexerSmartDefaultFactory(
					indexer.ReturnType, indexer.DefaultStrategy, indexer.ConcreteTypeForNew, indexer.InterceptorName);
				var ctorArgs = PreCompiledInterceptorRenderer.GetFieldConstructorArgs(indexer.InterceptorName, factory);
				w.Line($"/// <summary>Interceptor for indexer. Configure callbacks and track access.</summary>");
				w.Line($"public {newKeyword}{preCompiledType} {indexer.InterceptorName} {{ get; }} = new{ctorArgs};");
			}
			else
			{
				w.Line($"/// <summary>Interceptor for indexer. Configure callbacks and track access.</summary>");
				w.Line($"public {newKeyword}{indexer.InterceptorClassName} {indexer.InterceptorName} {{ get; }} = new();");
			}
			w.Line();
		}

		// Methods (only non-generic - generic methods use handler properties)
		// All method interceptors are fully generated classes inheriting MethodInterceptorRuntime.
		// Constructor takes no arguments; member name is hardcoded in the constructor via base("MethodName").
		foreach (var method in unit.Methods.Where(m => !m.IsGenericMethod))
		{
			if (!renderedProperties.Add(method.InterceptorName))
				continue;
			var newKeyword = method.NeedsNewKeyword ? "new " : "";
			w.Line($"/// <summary>Interceptor for {method.MethodName}.</summary>");
			w.Line($"public {newKeyword}{method.InterceptorClassName} {method.InterceptorName} {{ get; }} = new();");
			w.Line();
		}

		// Generic method handlers (never pre-compiled)
		foreach (var handler in unit.GenericMethodHandlers)
		{
			if (!renderedProperties.Add(handler.InterceptorName))
				continue;
			var newKeyword = handler.NeedsNewKeyword ? "new " : "";
			w.Line($"/// <summary>Interceptor for {handler.MethodName} (generic method).</summary>");
			w.Line($"public {newKeyword}{handler.InterceptorClassName} {handler.InterceptorName} {{ get; }} = new();");
			w.Line();
		}

		// Generic stub override handler groups (for overloaded generic stub overrides, never pre-compiled)
		foreach (var handlerGroup in unit.GenericStubOverrideHandlerGroups)
		{
			if (!renderedProperties.Add(handlerGroup.InterceptorName))
				continue;
			var newKeyword = handlerGroup.NeedsNewKeyword ? "new " : "";
			w.Line($"/// <summary>Interceptor for {handlerGroup.MethodName} (generic stub override with overloads).</summary>");
			w.Line($"public {newKeyword}{handlerGroup.InterceptorClassName} {handlerGroup.InterceptorName} {{ get; }} = new();");
			w.Line();
		}

		// Events (never pre-compiled - out of scope)
		foreach (var evt in unit.Events)
		{
			if (!renderedProperties.Add(evt.InterceptorName))
				continue;
			var newKeyword = evt.NeedsNewKeyword ? "new " : "";
			w.Line($"/// <summary>Interceptor for {evt.EventName} event.</summary>");
			w.Line($"public {newKeyword}{evt.InterceptorClassName} {evt.InterceptorName} {{ get; }} = new();");
			w.Line();
		}
	}

	#endregion

	#region Standard Members

	private static void RenderStandardMembers(CodeWriter w, FlatGenerationUnit unit)
	{
		// Strict mode property with configurable default
		var strictDefault = unit.Strict ? "true" : "false";
		w.Line("/// <summary>When true, throws StubException for unconfigured member access.</summary>");
		w.Line($"public bool Strict {{ get; set; }} = {strictDefault};");
		w.Line();

		// Object accessor - returns the first interface type
		var typeParams = FormatTypeParameters(unit.TypeParameters);
		var primaryInterface = unit.InterfaceList.Count > 0 ? unit.InterfaceList.GetArray()![0] : $"{unit.ClassName}{typeParams}";
		w.Line($"/// <summary>The {primaryInterface} instance. Use for passing to code expecting the interface.</summary>");
		w.Line($"public {primaryInterface} Object => this;");
		w.Line();

		// Verify and VerifyAll methods (if there are any verifiable members)
		// Must check all member types: methods, stub overrides, properties, events
		// NOTE: Indexers excluded from Verify/VerifyAll - will be added in the indexer API redesign
		if (unit.MethodGroups.Count > 0
			|| unit.StubOverrideGroups.Count > 0
			|| unit.Properties.Count > 0
			|| unit.Events.Count > 0)
		{
			RenderVerifyMethods(w, unit);
		}

		// SmartDefault helper (only if there are generic methods)
		if (unit.HasGenericMethods)
		{
			RenderSmartDefaultMethod(w);
		}
	}

	private static void RenderVerifyMethods(CodeWriter w, FlatGenerationUnit unit)
	{
		// Get unique interceptor names for methods (method groups define the interceptor names)
		var methodInterceptorNames = unit.MethodGroups
			.Select(g => g.InterceptorName)
			.Distinct()
			.ToList();

		// Get unique user-defined method interceptor names (tracking-only, always configured)
		var stubOverrideInterceptorNames = unit.StubOverrideGroups
			.Select(g => g.InterceptorName)
			.Distinct()
			.ToList();

		// Get unique property interceptor names
		var propertyInterceptorNames = unit.Properties
			.Select(p => p.InterceptorName)
			.Distinct()
			.ToList();

		// Get unique indexer interceptor names
		var indexerInterceptorNames = unit.Indexers
			.Select(i => i.InterceptorName)
			.Distinct()
			.ToList();

		// Get unique event interceptor names
		var eventInterceptorNames = unit.Events
			.Select(e => e.InterceptorName)
			.Distinct()
			.ToList();

		// Verify method - checks only .Verifiable() items, throws if any fail
		w.Line("/// <summary>Verifies all members marked with .Verifiable() were invoked as expected. Throws VerificationException with all failures if any fail.</summary>");
		using (w.Block("public void Verify()"))
		{
			w.Line("var failures = new global::System.Collections.Generic.List<global::KnockOff.VerificationFailure>();");
			w.Line();

			// Check verifiable method interceptors
			foreach (var name in methodInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerification() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check verifiable property interceptors
			foreach (var name in propertyInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerification() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check verifiable indexer interceptors
			foreach (var name in indexerInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerification() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check verifiable event interceptors
			foreach (var name in eventInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerification() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check verifiable user-defined method interceptors
			foreach (var name in stubOverrideInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerification() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			w.Line();
			w.Line("if (failures.Count > 0)");
			w.Line("\tthrow new global::KnockOff.VerificationException(failures);");
		}
		w.Line();

		// VerifyAll method - checks ALL configured members, throws if any fail
		// NOTE: User-defined methods are NOT included in VerifyAll because they are always "configured"
		w.Line("/// <summary>Verifies ALL configured members were invoked at least once. Throws VerificationException with all failures if any fail.</summary>");
		using (w.Block("public void VerifyAll()"))
		{
			w.Line("var failures = new global::System.Collections.Generic.List<global::KnockOff.VerificationFailure>();");
			w.Line();

			// Check all configured method interceptors
			foreach (var name in methodInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerificationAll() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check all configured property interceptors
			foreach (var name in propertyInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerificationAll() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check all configured indexer interceptors
			foreach (var name in indexerInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerificationAll() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			// Check all configured event interceptors
			foreach (var name in eventInterceptorNames)
			{
				w.Line($"if ({name}.CheckVerificationAll() is {{ }} {name.ToLowerInvariant()}Failure) failures.Add({name.ToLowerInvariant()}Failure);");
			}

			w.Line();
			w.Line("if (failures.Count > 0)");
			w.Line("\tthrow new global::KnockOff.VerificationException(failures);");
		}
		w.Line();
	}

	private static void RenderSmartDefaultMethod(CodeWriter w)
	{
		w.Line("/// <summary>Gets a smart default value for a generic type at runtime.</summary>");
		using (w.Block("private static TSmartDefault SmartDefault<TSmartDefault>(string methodName)"))
		{
			w.Line("var type = typeof(TSmartDefault);");
			w.Line();
			w.Line("// Value types -> default(TSmartDefault)");
			w.Line("if (type.IsValueType)");
			w.Line("\treturn default!;");
			w.Line();
			w.Line("// Check for parameterless constructor");
			w.Line("var ctor = type.GetConstructor(");
			w.Line("\tSystem.Reflection.BindingFlags.Public | System.Reflection.BindingFlags.Instance,");
			w.Line("\tnull, System.Type.EmptyTypes, null);");
			w.Line();
			w.Line("if (ctor != null)");
			w.Line("\treturn (TSmartDefault)ctor.Invoke(null);");
			w.Line();
			w.Line("throw new global::System.InvalidOperationException(");
			w.Line("\t$\"No implementation provided for {methodName}<{type.Name}>. \" +");
			w.Line("\t$\"Set the handler's Return or Call.\");");
		}
		w.Line();
	}

	#endregion

	#region Source Methods

	/// <summary>
	/// Renders Source(T) methods for each interface in the inheritance hierarchy.
	/// Each Source(T) overload sets _source for members declared on T or its bases,
	/// and clears _source for members declared on more derived interfaces.
	/// </summary>
	private static void RenderSourceMethods(CodeWriter w, FlatGenerationUnit unit, Dictionary<string, string> preCompiledInterceptors)
	{
		if (unit.SourceProviders.Count == 0)
			return;

		w.Line("// Source(T) methods for interface delegation");
		w.Line();

		foreach (var provider in unit.SourceProviders)
		{
			w.Line($"/// <summary>Delegates unconfigured member access to the provided source object ({provider.InterfaceType}).</summary>");
			w.Line($"/// <param name=\"source\">The source to delegate to, or null to clear.</param>");
			using (w.Block($"public void {provider.MethodName}({provider.InterfaceType}? source)"))
			{
				foreach (var mapping in provider.MemberMappings)
				{
					if (preCompiledInterceptors.ContainsKey(mapping.InterceptorName))
					{
						// Pre-compiled: use SetSourceFallback with method group or lambda
						if (mapping.SetSource)
						{
							// Determine member kind from the mapping
							var memberKind = GetSourceMemberKind(unit, mapping.InterceptorName);
							switch (memberKind)
							{
								case SourceMemberKind.Method:
									var methodInfo = GetMethodInfoForInterceptor(unit, mapping.InterceptorName);
									if (methodInfo != null)
									{
										// Use interface disambiguation when the method name is ambiguous (diamond inheritance)
										var disambigInterface = NeedsInterfaceDisambiguation(unit, mapping.InterceptorName, provider.InterfaceType)
											? methodInfo.Value.DeclaringInterface
											: null;
										w.Line(PreCompiledInterceptorRenderer.GetMethodSourceFallbackExpression(
											mapping.InterceptorName,
											methodInfo.Value.MethodName,
											"source",
											methodInfo.Value.Parameters,
											methodInfo.Value.ReturnType,
											methodInfo.Value.IsVoid,
											disambigInterface,
											mapping.InterceptorName));
									}
									break;
								case SourceMemberKind.Property:
									var propInfo = GetPropertyInfoForInterceptor(unit, mapping.InterceptorName);
									if (propInfo != null)
									{
										w.Line(PreCompiledInterceptorRenderer.GetPropertySourceFallbackExpression(
											mapping.InterceptorName,
											propInfo.Value.PropertyName,
											"source",
											propInfo.Value.HasGetter,
											propInfo.Value.HasSetter));
									}
									break;
								case SourceMemberKind.Indexer:
									var indexerInfo = GetIndexerInfoForInterceptor(unit, mapping.InterceptorName);
									if (indexerInfo != null)
									{
										w.Line(PreCompiledInterceptorRenderer.GetIndexerSourceFallbackExpression(
											mapping.InterceptorName,
											"source",
											provider.InterfaceType,
											indexerInfo.Value.KeyParamName,
											indexerInfo.Value.HasGetter,
											indexerInfo.Value.HasSetter));
									}
									break;
							}
						}
						else
						{
							// Clear source fallback: use typed null for async methods to avoid ambiguity
							var memberKind = GetSourceMemberKind(unit, mapping.InterceptorName);
							switch (memberKind)
							{
								case SourceMemberKind.Method:
									var methodInfoClear = GetMethodInfoForInterceptor(unit, mapping.InterceptorName);
									if (methodInfoClear != null)
									{
										w.Line(PreCompiledInterceptorRenderer.GetMethodSourceFallbackClearExpression(
											mapping.InterceptorName,
											methodInfoClear.Value.Parameters,
											methodInfoClear.Value.ReturnType,
											methodInfoClear.Value.IsVoid));
									}
									break;
								case SourceMemberKind.Property:
									var propInfo = GetPropertyInfoForInterceptor(unit, mapping.InterceptorName);
									if (propInfo != null)
									{
										if (propInfo.Value.HasGetter && propInfo.Value.HasSetter)
											w.Line($"{mapping.InterceptorName}.SetSourceFallback(null, null);");
										else
											w.Line($"{mapping.InterceptorName}.SetSourceFallback(null);");
									}
									break;
								case SourceMemberKind.Indexer:
									w.Line($"{mapping.InterceptorName}.SetSourceFallback(null, null);");
									break;
							}
						}
					}
					else
					{
						// Fallback: use existing _source assignment
						if (mapping.SetSource)
						{
							w.Line($"{mapping.InterceptorName}._source = source;");
						}
						else
						{
							w.Line($"{mapping.InterceptorName}._source = null;");
						}
					}
				}
			}
			w.Line();
		}
	}

	private enum SourceMemberKind { Method, Property, Indexer, Unknown }

	private static SourceMemberKind GetSourceMemberKind(FlatGenerationUnit unit, string interceptorName)
	{
		if (unit.Methods.Any(m => m.InterceptorName == interceptorName && !m.IsGenericMethod))
			return SourceMemberKind.Method;
		if (unit.Properties.Any(p => p.InterceptorName == interceptorName))
			return SourceMemberKind.Property;
		if (unit.Indexers.Any(i => i.InterceptorName == interceptorName))
			return SourceMemberKind.Indexer;
		return SourceMemberKind.Unknown;
	}

	private static (string MethodName, IEnumerable<ParameterModel> Parameters, string ReturnType, bool IsVoid, string DeclaringInterface)?
		GetMethodInfoForInterceptor(FlatGenerationUnit unit, string interceptorName)
	{
		var method = unit.Methods.FirstOrDefault(m => m.InterceptorName == interceptorName && !m.IsGenericMethod);
		if (method == null) return null;
		return (method.MethodName, method.Parameters.AsEnumerable(), method.ReturnType, method.IsVoid, method.DeclaringInterface);
	}

	/// <summary>
	/// Determines whether a method needs interface-qualified source delegation
	/// (i.e., the method name is ambiguous from diamond inheritance).
	/// </summary>
	private static bool NeedsInterfaceDisambiguation(FlatGenerationUnit unit, string interceptorName, string providerInterface)
	{
		// If the provider interface matches the method's declaring interface, no issue.
		// But if the method is inherited from multiple base interfaces, the source
		// expression `source.MethodName` would be ambiguous.
		var matchingMethods = unit.Methods
			.Where(m => m.InterceptorName == interceptorName && !m.IsGenericMethod)
			.ToList();
		// Check if there are multiple explicit implementations for the same interceptor
		// (this happens with diamond inheritance where IFoo.DoWork and IBar.DoWork are both present)
		return matchingMethods.Count > 1;
	}

	private static (string PropertyName, bool HasGetter, bool HasSetter)?
		GetPropertyInfoForInterceptor(FlatGenerationUnit unit, string interceptorName)
	{
		var prop = unit.Properties.FirstOrDefault(p => p.InterceptorName == interceptorName);
		if (prop == null) return null;
		return (prop.MemberName, prop.HasGetter, prop.HasSetter);
	}

	private static (string KeyParamName, bool HasGetter, bool HasSetter)?
		GetIndexerInfoForInterceptor(FlatGenerationUnit unit, string interceptorName)
	{
		var indexer = unit.Indexers.FirstOrDefault(i => i.InterceptorName == interceptorName);
		if (indexer == null) return null;
		return (indexer.KeyParamName, indexer.HasGetter, indexer.HasSetter);
	}

	#endregion

	#region Constructor for Stub Override Fallback

	private static void RenderConstructorIfNeeded(
		CodeWriter w,
		FlatGenerationUnit unit,
		Dictionary<string, string> preCompiledInterceptors,
		string classNameWithTypeParams)
	{
		// Collect stub override fallback wiring needed for pre-compiled interceptors
		var fallbackWiring = new List<string>();

		// Method stub overrides
		foreach (var group in unit.StubOverrideGroups)
		{
			if (!preCompiledInterceptors.ContainsKey(group.InterceptorName))
				continue;
			var methods = group.Methods.GetArray();
			if (methods == null || methods.Length == 0) continue;
			var first = methods[0];
			if (first.HasStubOverride && !string.IsNullOrEmpty(GetStubOverrideName(first)))
			{
				fallbackWiring.Add(PreCompiledInterceptorRenderer.GetStubOverrideFallbackExpression(
					group.InterceptorName, GetStubOverrideName(first)!,
					first.ReturnType, first.Parameters.AsEnumerable()));
			}
		}

		// Property stub overrides
		foreach (var prop in unit.Properties)
		{
			if (!preCompiledInterceptors.ContainsKey(prop.InterceptorName))
				continue;
			if (prop.HasStubOverride)
			{
				fallbackWiring.Add(PreCompiledInterceptorRenderer.GetPropertyStubOverrideFallbackExpression(
					prop.InterceptorName, prop.MemberName, prop.HasGetter, prop.HasSetter));
			}
		}

		if (fallbackWiring.Count == 0)
			return;

		// When the user's class uses a primary constructor (C# 12 parameter list),
		// any generated constructor must include `: this()` initializer (CS8862).
		var ctorInitializer = unit.HasPrimaryConstructor ? " : this()" : "";
		w.Line($"/// <summary>Wires stub override fallbacks to pre-compiled interceptors.</summary>");
		using (w.Block($"public {ExtractSimpleClassName(classNameWithTypeParams)}(){ctorInitializer}"))
		{
			foreach (var wiring in fallbackWiring)
			{
				w.Line(wiring);
			}
		}
		w.Line();
	}

	private static string? GetStubOverrideName(FlatMethodModel method)
	{
		// The stub override method is named MethodName_ by convention
		return method.HasStubOverride ? $"{method.MethodName}_" : null;
	}

	private static string ExtractSimpleClassName(string classNameWithTypeParams)
	{
		var idx = classNameWithTypeParams.IndexOf('<');
		return idx >= 0 ? classNameWithTypeParams.Substring(0, idx) : classNameWithTypeParams;
	}

	#endregion

	#region Property Implementation

	private static void RenderPropertyImplementation(CodeWriter w, FlatPropertyModel prop, Dictionary<string, string> preCompiledInterceptors)
	{
		// Handle property delegation (e.g., IProperty.Value (object) delegates to IProperty<T>.Value (T))
		if (prop.DelegationTarget != null && prop.DelegationTargetInterface != null)
		{
			RenderPropertyDelegation(w, prop);
			return;
		}

		// User-defined properties with base class pattern: record access, check Get/Set, then delegate to virtual property
		// For pre-compiled interceptors, the fallback is wired via SetFallback in the constructor,
		// so we use InvokeGet/InvokeSet which handle the full priority chain including fallback.
		if (prop.HasStubOverride && !preCompiledInterceptors.ContainsKey(prop.InterceptorName))
		{
			RenderPropertyStubOverrideImplementation(w, prop);
			return;
		}

		w.Line($"{prop.RefReturnPrefix}{prop.ReturnType} {prop.DeclaringInterface}.{prop.MemberName}");
		using (w.Braces())
		{
			if (prop.IsRefReturn)
			{
				// Ref return properties are always get-only (C# constraint)
				w.Line("get");
				using (w.Braces())
				{
					w.Line($"{prop.InterceptorName}.InvokeRefGet(Strict);");
					w.Line($"return ref {prop.InterceptorName}._refReturnBacking;");
				}
			}
			else if (prop.IsInitOnly)
			{
				// Init-only: use InvokeGet for getter, but init setter records and sets value via SetValue
				w.Line($"get => {prop.InterceptorName}.InvokeGet(Strict);");
				w.Line($"init {{ {prop.InterceptorName}.RecordSet(value); {prop.InterceptorName}.SetValue(value); }}");
			}
			else
			{
				// Regular properties: use InvokeGet/InvokeSet which handle the priority chain
				if (prop.HasGetter)
				{
					w.Line($"get => {prop.InterceptorName}.InvokeGet(Strict);");
				}

				if (prop.HasSetter)
				{
					if (prop.SetterPragmaDisable != null)
						w.Append(prop.SetterPragmaDisable);
					w.Line($"set => {prop.InterceptorName}.InvokeSet(Strict, value);");
					if (prop.SetterPragmaRestore != null)
						w.Line(prop.SetterPragmaRestore);
				}
			}
		}
		w.Line();
	}

	/// <summary>
	/// Renders the explicit interface implementation for a property with stub override (base class pattern).
	/// Priority chain: Get/Set > Stub override (virtual property with _ suffix).
	/// Unlike properties without stub override, this does NOT fall through to Strict/Default.
	/// </summary>
	private static void RenderPropertyStubOverrideImplementation(CodeWriter w, FlatPropertyModel prop)
	{
		w.Line($"{prop.ReturnType} {prop.DeclaringInterface}.{prop.MemberName}");
		using (w.Braces())
		{
			if (prop.HasGetter)
			{
				w.Line("get");
				using (w.Braces())
				{
					// Get supersedes stub override (InvokeGetCallback tracks internally)
					w.Line($"if ({prop.InterceptorName}.HasGet) return {prop.InterceptorName}.InvokeGetCallback();");
					// Record access only for stub override path (to avoid double counting)
					w.Line($"{prop.InterceptorName}.RecordGet();");
					// Stub override (virtual property with _ suffix)
					w.Line($"return {prop.MemberName}_;");
				}
			}

			if (prop.HasSetter)
			{
				if (prop.SetterPragmaDisable != null)
					w.Append(prop.SetterPragmaDisable);
				w.Line("set");
				using (w.Braces())
				{
					// Set supersedes stub override (InvokeSetCallback tracks internally)
					w.Line($"if ({prop.InterceptorName}.HasSet) {{ {prop.InterceptorName}.InvokeSetCallback(value); return; }}");
					// Record access only for stub override path (to avoid double counting)
					w.Line($"{prop.InterceptorName}.RecordSet(value);");
					// Stub override (virtual property with _ suffix)
					w.Line($"{prop.MemberName}_ = value;");
				}
				if (prop.SetterPragmaRestore != null)
					w.Line(prop.SetterPragmaRestore);
			}
		}
		w.Line();
	}

	private static void RenderPropertyDelegation(CodeWriter w, FlatPropertyModel prop)
	{
		var target = prop.DelegationTarget!;
		var targetInterface = prop.DelegationTargetInterface!;

		w.Line($"{prop.ReturnType} {prop.DeclaringInterface}.{prop.MemberName}");
		using (w.Braces())
		{
			if (prop.HasGetter)
			{
				// Cast the typed value to object (or the base return type)
				w.Line($"get => (({targetInterface})this).{target.Name}!;");
			}

			if (prop.HasSetter)
			{
				// Cast the object value to the typed type
				w.Line($"set => (({targetInterface})this).{target.Name} = ({target.ReturnType.TrimEnd('?')})value!;");
			}
		}
		w.Line();
	}

	#endregion

	#region Indexer Implementation

	private static void RenderIndexerImplementation(CodeWriter w, FlatIndexerModel indexer, HashSet<string> multiIndexerInterceptors)
	{
		var accessExpr = indexer.InterceptorName;
		var isMulti = multiIndexerInterceptors.Contains(indexer.InterceptorName);
		var invokeSuffix = isMulti ? $"_{indexer.KeyTypeFriendlyName}" : "";

		w.Line($"{indexer.RefReturnPrefix}{indexer.ReturnType} {indexer.DeclaringInterface}.this[{indexer.ParameterSignature}]");
		using (w.Braces())
		{
			if (indexer.IsRefReturn)
			{
				// Ref return indexers are always get-only (C# constraint)
				w.Line("get");
				using (w.Braces())
				{
					w.Line($"{accessExpr}.InvokeRefGet{invokeSuffix}(Strict, {indexer.ArgumentList});");
					w.Line($"return ref {accessExpr}._refReturnBacking{invokeSuffix};");
				}
			}
			else
			{
				// Use InvokeGet/InvokeSet which handle the priority chain
				if (indexer.HasGetter)
				{
					w.Line($"get => {accessExpr}.InvokeGet{invokeSuffix}(Strict, {indexer.ArgumentList});");
				}

				if (indexer.HasSetter)
				{
					var setterKeyword = indexer.IsInitOnly ? "init" : "set";
					w.Line($"{setterKeyword} => {accessExpr}.InvokeSet{invokeSuffix}(Strict, {indexer.ArgumentList}, value);");
				}
			}
		}
		w.Line();
	}

	#endregion

	#region Method Implementation

	private static void RenderMethodImplementation(
		CodeWriter w,
		FlatMethodModel method,
		HashSet<string> multiOverloadInterceptors,
		HashSet<string> multiOverloadStubOverrideInterceptors,
		HashSet<string> multiOverloadGenericStubOverrideInterceptors,
		EquatableArray<FlatGenericMethodHandlerGroup> genericStubOverrideHandlerGroups,
		Dictionary<string, string> preCompiledInterceptors)
	{
		// Handle method delegation (e.g., IRule.RunRule(IValidateBase) delegates to IRule<T>.RunRule(T))
		if (method.DelegationTarget != null && method.DelegationTargetInterface != null)
		{
			RenderMethodDelegation(w, method);
			return;
		}

		// Generic methods use method-based Return/Call API via typed handlers (never pre-compiled)
		if (method.IsGenericMethod)
		{
			RenderGenericMethodImplementation(w, method, multiOverloadGenericStubOverrideInterceptors, genericStubOverrideHandlerGroups);
			return;
		}

		// User-defined methods with base class pattern
		if (method.HasStubOverride)
		{
			RenderStubOverrideImplementation(w, method, multiOverloadStubOverrideInterceptors);
			return;
		}

		// Determine if this method is part of an overload group (needs suffixed Invoke)
		var isMultiOverload = multiOverloadInterceptors.Contains(method.InterceptorName);
		var invokeSuffix = isMultiOverload ? $"_{GetSignatureSuffix(method)}" : "";

		// All methods use the generated interceptor class's Invoke method
		w.Line($"{method.RefReturnPrefix}{method.ReturnType} {method.DeclaringInterface}.{method.MethodName}({method.ParameterDeclarations})");
		using (w.Braces())
		{
			var invokeArgs = method.Parameters.Count > 0
				? "Strict, " + string.Join(", ", method.Parameters.Select(p => $"{p.RefPrefix}{p.EscapedName}"))
				: "Strict";

			if (method.IsRefReturn)
			{
				var invokeRefSuffix = isMultiOverload ? $"_{GetSignatureSuffix(method)}" : "";
				var backingField = isMultiOverload ? $"_refReturnBacking_{GetSignatureSuffix(method)}" : "_refReturnBacking";
				w.Line($"{method.InterceptorName}.InvokeRef{invokeRefSuffix}({invokeArgs});");
				w.Line($"return ref {method.InterceptorName}.{backingField};");
			}
			else if (method.IsVoid)
				w.Line($"{method.InterceptorName}.Invoke{invokeSuffix}({invokeArgs});");
			else
				w.Line($"return {method.InterceptorName}.Invoke{invokeSuffix}({invokeArgs});");
		}
		w.Line();
	}

	/// <summary>
	/// Renders the explicit interface implementation for a method with stub override (base class pattern).
	/// Priority chain: When chains > Sequences > Return/Call > Stub override (virtual method with _ suffix).
	/// The unified interceptor handles all logic including stub override fallback.
	/// </summary>
	private static void RenderStubOverrideImplementation(CodeWriter w, FlatMethodModel method, HashSet<string> multiOverloadStubOverrideInterceptors)
	{
		// Determine if this stub override is part of an overload group (needs suffixed Invoke)
		var isMultiOverload = multiOverloadStubOverrideInterceptors.Contains(method.InterceptorName);
		var invokeSuffix = isMultiOverload ? $"_{GetSignatureSuffix(method)}" : "";

		w.Line($"{method.ReturnType} {method.DeclaringInterface}.{method.MethodName}({method.ParameterDeclarations})");
		using (w.Braces())
		{
			// Build invoke args (includes Strict, stub reference, and method parameters)
			var invokeArgs = method.Parameters.Count > 0
				? "Strict, this, " + string.Join(", ", method.Parameters.Select(p => $"{p.RefPrefix}{p.EscapedName}"))
				: "Strict, this";

			if (method.IsVoid)
				w.Line($"{method.InterceptorName}.Invoke{invokeSuffix}({invokeArgs});");
			else
				w.Line($"return {method.InterceptorName}.Invoke{invokeSuffix}({invokeArgs});");
		}
		w.Line();
	}

	private static void RenderGenericMethodImplementation(
		CodeWriter w,
		FlatMethodModel method,
		HashSet<string> multiOverloadGenericStubOverrideInterceptors,
		EquatableArray<FlatGenericMethodHandlerGroup> genericStubOverrideHandlerGroups)
	{
		// For methods with unconstrained nullable type parameters (T? without where T : class),
		// we must disable nullable context. Without this, the compiler interprets T? as Nullable<T>
		// (CS0453) or reports nullability mismatch (CS8769). The type strings are already stripped
		// of ? for these type parameters by the builder.
		if (method.NeedsNullableDisable)
			w.Line("#nullable disable");

		// Generic methods use the method-based Return/Call API via typed handlers
		w.Line($"{method.ReturnType} {method.DeclaringInterface}.{method.MethodName}{method.TypeParameterDecl}({method.ParameterDeclarations}){method.ConstraintClauses}");
		using (w.Braces())
		{
			// Initialize out parameters
			foreach (var p in method.Parameters.Where(p => p.RefKind == Microsoft.CodeAnalysis.RefKind.Out))
			{
				w.Line($"{p.EscapedName} = default!;");
			}

			var interceptorAccess = $"{method.InterceptorName}{method.OfTypeAccess}";

			// Generic methods use RecordCallArgs (excludes generic-typed params)
			var recordCallArgs = method.RecordCallArgs;

			// Record the call
			w.Line($"{interceptorAccess}.RecordCall({recordCallArgs});");

			// Build onCall args (no stub parameter)
			var callArgs = method.Parameters.Count > 0
				? string.Join(", ", method.Parameters.Select(p => $"{p.RefPrefix}{p.EscapedName}"))
				: "";

			if (method.IsVoid)
			{
				w.Line($"if ({interceptorAccess}.Callback is {{ }} callCallback)");
				w.Line($"{{ callCallback({callArgs}); return; }}");
				w.Line($"if (Strict) throw global::KnockOff.StubException.NotConfigured(\"{method.SimpleInterfaceName}\", \"{method.MethodName}\");");
				// Generic methods don't support stub overrides - just return for void
			}
			else if (method.ThrowsOnDefault)
			{
				w.Line($"if ({interceptorAccess}.Callback is {{ }} callback)");
				w.Line($"\treturn callback({callArgs});");
				w.Line($"if (Strict) throw global::KnockOff.StubException.NotConfigured(\"{method.SimpleInterfaceName}\", \"{method.MethodName}\");");
				w.Line($"throw new global::System.InvalidOperationException(\"No implementation provided for {method.MethodName}. Use {interceptorAccess}.{(method.IsVoid ? "Call" : "Return")}(callback).\");");
			}
			else
			{
				w.Line($"if ({interceptorAccess}.Callback is {{ }} callback)");
				w.Line($"\treturn callback({callArgs});");
				w.Line($"if (Strict) throw global::KnockOff.StubException.NotConfigured(\"{method.SimpleInterfaceName}\", \"{method.MethodName}\");");
				w.Line($"return {method.DefaultExpression};");
			}
		}

		if (method.NeedsNullableDisable)
			w.Line("#nullable restore");
		w.Line();
	}

	/// <summary>
	/// Gets the signature suffix for a generic stub override from the handler groups.
	/// </summary>
	private static string GetGenericStubOverrideSignatureSuffix(
		FlatMethodModel method,
		EquatableArray<FlatGenericMethodHandlerGroup> handlerGroups)
	{
		var group = handlerGroups.FirstOrDefault(g => g.InterceptorName == method.InterceptorName);
		if (group == null)
			return "";

		// Strip angle brackets from method's type parameter list for comparison
		// TypeParameterList has angle brackets (e.g., "<T>"), TypeParameterNames does not (e.g., "T")
		var methodTypeParams = method.TypeParameterList.TrimStart('<').TrimEnd('>');

		// Find the type arity group matching this method's type parameters
		var arityGroup = group.TypeArityGroups.FirstOrDefault(a => a.TypeParameterNames == methodTypeParams);
		if (arityGroup == null)
			return "";

		// Find the signature group matching this method
		// We need to match by parameter types
		var paramArray = method.Parameters.GetArray() ?? Array.Empty<ParameterModel>();
		foreach (var sig in arityGroup.SignatureGroups)
		{
			var sigParams = sig.AllParameters.GetArray() ?? Array.Empty<ParameterModel>();
			if (paramArray.Length == sigParams.Length)
			{
				var match = true;
				for (int i = 0; i < paramArray.Length; i++)
				{
					if (paramArray[i].Type != sigParams[i].Type)
					{
						match = false;
						break;
					}
				}
				if (match)
					return sig.SignatureSuffix;
			}
		}

		return "";
	}

	private static void RenderMethodDelegation(CodeWriter w, FlatMethodModel method)
	{
		var target = method.DelegationTarget!;
		var targetInterface = method.DelegationTargetInterface!;

		// Build parameter cast expressions for delegation
		var memberParams = method.Parameters.GetArray() ?? System.Array.Empty<KnockOff.Model.Shared.ParameterModel>();
		var targetParams = target.Parameters.GetArray() ?? System.Array.Empty<ParameterInfo>();

		var castArgs = new List<string>();
		for (int i = 0; i < memberParams.Length; i++)
		{
			var memberType = memberParams[i].Type;
			var targetType = targetParams[i].Type;
			var paramName = memberParams[i].EscapedName;

			if (memberType != targetType)
			{
				// Cast to the target type
				castArgs.Add($"({targetType}){paramName}");
			}
			else
			{
				castArgs.Add(paramName);
			}
		}

		var argList = string.Join(", ", castArgs);

		// Expression-bodied members don't use 'return' keyword
		if (method.NeedsNullableDisable)
			w.Line("#nullable disable");
		w.Line($"{method.ReturnType} {method.DeclaringInterface}.{method.MethodName}{method.TypeParameterDecl}({method.ParameterDeclarations}){method.ConstraintClauses}");
		w.Line($"\t=> (({targetInterface})this).{target.Name}({argList});");
		if (method.NeedsNullableDisable)
			w.Line("#nullable restore");
		w.Line();
	}

	#endregion

	#region Event Implementation

	private static void RenderEventImplementation(CodeWriter w, FlatEventModel evt)
	{
		w.Line($"event {evt.DelegateType}? {evt.DeclaringInterface}.{evt.EventName}");
		using (w.Braces())
		{
			w.Line($"add => {evt.InterceptorName}.RecordAdd(value);");
			w.Line($"remove => {evt.InterceptorName}.RecordRemove(value);");
		}
		w.Line();
	}

	#endregion

	#region Async Type Helpers

	/// <summary>
	/// Analyzes a return type for async patterns and extracts the inner type.
	/// Used for Returns() auto-wrapping in stub override interceptors.
	/// </summary>
	private static (string InnerType, bool IsTaskT, bool IsValueTaskT) GetAsyncTypeInfo(string returnType)
	{
		const string TaskPrefix = "global::System.Threading.Tasks.Task<";
		const string ValueTaskPrefix = "global::System.Threading.Tasks.ValueTask<";

		if (returnType.StartsWith(TaskPrefix) && returnType.EndsWith(">"))
		{
			var innerType = returnType.Substring(TaskPrefix.Length, returnType.Length - TaskPrefix.Length - 1);
			return (innerType, true, false);
		}

		if (returnType.StartsWith(ValueTaskPrefix) && returnType.EndsWith(">"))
		{
			var innerType = returnType.Substring(ValueTaskPrefix.Length, returnType.Length - ValueTaskPrefix.Length - 1);
			return (innerType, false, true);
		}

		return (returnType, false, false);
	}

	#endregion
}
